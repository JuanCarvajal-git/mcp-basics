[project]
name = "mcp-basics"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Juan Carvajal", email = "juanmcarvajaleia@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "mcp[cli]>=2.2.0",
]

[project.scripts]
mcp-basics = "mcp_basics:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
